# ChatMood web — Next.js inlines NEXT_PUBLIC_* at build time.
# Local dev defaults work without this file.
# Netlify/Vercel: set these in the dashboard → Site settings → Environment variables.
NEXT_PUBLIC_API_URL=https://api.your-domain.com/api/v1
NEXT_PUBLIC_SITE_URL=https://moodai-app.vercel.app
